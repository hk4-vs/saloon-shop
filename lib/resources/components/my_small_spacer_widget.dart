import 'package:flutter/material.dart';

class MySmallSpacerWidget extends StatelessWidget {
  const MySmallSpacerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
