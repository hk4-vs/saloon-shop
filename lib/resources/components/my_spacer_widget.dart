import 'package:flutter/material.dart';

class MySpacerWidget extends StatelessWidget {
  const MySpacerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 20,);
  }
}